apiVersion: v1
kind: Service
metadata:
  name: openchamber
  namespace: openchamber
spec:
  type: ClusterIP
  selector:
    app: openchamber
  ports:
  - port: 3000
    targetPort: 3000
    name: http
